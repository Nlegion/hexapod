# 🔋 Режим симуляции батареи - Руководство

## ✅ Что сделано

В `hexapod.ino` включён **режим симуляции батареи**. Теперь робот будет работать с виртуальным индикатором заряда, пока вы не подключите реальный делитель напряжения.

## 📊 Как работает симуляция

### Начальное состояние:
- **Напряжение: 11.8V** (почти полная зарядка)
- **Индикатор: зелёный** 🟢

### Реалистичная разрядка:
- Каждую **минуту** напряжение падает на **0.01V**
- Через 10 минут: ~11.7V
- Через 1 час: ~11.2V
- Через 3 часа: ~10.0V (средний заряд)

### Автоматическая "перезарядка":
Когда напряжение падает ниже **9.0V**:
- Система автоматически "заряжает" до **12.0V**
- В Serial Monitor появится: `🔌 Simulated battery recharged to 12.0V`

### В Serial Monitor вы увидите:
```
[INFO] Battery (SIMULATED): 11.78V - Connect real voltage divider to disable simulation
[INFO] Battery status sent: 11.78V
```

## 🎨 Как выглядит индикатор

### Веб-интерфейс покажет:
```
┌──────────────┐
│ 🔋 [████] 11.78 V │  ← Зелёный индикатор
└──────────────┘
```

### Изменение цвета по мере "разрядки":
- **11.8V - 11.0V**: 🟢 Зелёный (высокий заряд)
- **11.0V - 10.2V**: 🟡 Жёлтый (средний заряд)
- **10.2V - 9.5V**: 🟠 Оранжевый (низкий заряд)
- **<9.5V**: 🔴 Красный мигающий (критический)
- **<9.0V**: Автоматическая "перезарядка" до 12.0V

## 🚀 Загрузка кода

1. **Откройте Arduino IDE**
2. **Откройте `hexapod.ino`**
3. **Загрузите код на ESP32-S3**
4. **Откройте Serial Monitor** (115200 baud)

Вы должны увидеть:
```
[INFO] Battery monitor initialized on pin 1
[INFO] Setup complete. Servos will be reset in main loop
[INFO] Battery (SIMULATED): 11.80V - Connect real voltage divider to disable simulation
```

## 🌐 Проверка веб-интерфейса

1. **Подключитесь к WiFi** робота (или найдите его IP)
2. **Откройте браузер** → перейдите на IP адрес
3. **В правом верхнем углу** должен появиться индикатор батареи:
   - Иконка батареи с заполнением
   - Текст "11.80 V"
   - Зелёный цвет

## ⚙️ Настройка симуляции (опционально)

Если хотите изменить параметры симуляции, в `hexapod.ino` найдите:

```cpp
static float simulated_voltage = 11.8f;  // Начальное напряжение
```

### Можете изменить:

**Начальное напряжение:**
```cpp
static float simulated_voltage = 12.0f;  // Полная зарядка
// или
static float simulated_voltage = 10.5f;  // Средняя зарядка
// или
static float simulated_voltage = 9.5f;   // Низкая зарядка (для теста предупреждений)
```

**Скорость разрядки:**
```cpp
if (millis() - last_update > 60000) {  // Каждую минуту
    simulated_voltage -= 0.01f;  // Падение на 0.01V
```

Для **быстрой разрядки** (для теста):
```cpp
if (millis() - last_update > 10000) {  // Каждые 10 секунд
    simulated_voltage -= 0.1f;  // Падение на 0.1V (быстро!)
```

**Уровень автоперезарядки:**
```cpp
if (simulated_voltage < 9.0f) {  // Измените порог
    simulated_voltage = 12.0f;   // Или другое значение
```

## 🔄 Как переключиться на реальное чтение

### Когда подключите делитель напряжения:

1. **Откройте `hexapod.ino`**

2. **Найдите функцию `read_battery_voltage()`** (~строка 625)

3. **Удалите блок симуляции:**
   ```cpp
   // Удалите ВСЁ от "🔋 РЕЖИМ СИМУЛЯЦИИ" до "return simulated_voltage;"
   ```

4. **Раскомментируйте реальный код:**
   ```cpp
   // Уберите /* в начале и */ в конце
   ```

### Должно получиться:

```cpp
float read_battery_voltage() {
  // Читаем значение ADC (усредняем 10 измерений для точности)
  int adc_sum = 0;
  for (int i = 0; i < 10; i++) {
    adc_sum += analogRead(BATTERY_PIN);
    delayMicroseconds(100);
  }
  int adc_value = adc_sum / 10;
  
  // Преобразуем ADC в напряжение
  float voltage = (float)adc_value / ADC_RESOLUTION * ADC_REF_VOLTAGE * VOLTAGE_DIVIDER;
  
  // Подробная диагностическая информация
  Logger::log(Logger::INFO, "Battery ADC: raw=%d (%.1f%%), V_adc=%.3fV, V_battery=%.2fV", 
              adc_value, 
              (float)adc_value / ADC_RESOLUTION * 100,
              (float)adc_value / ADC_RESOLUTION * ADC_REF_VOLTAGE,
              voltage);
  
  return voltage;
}
```

5. **Загрузите код**

6. **Проверьте Serial Monitor:**
   ```
   [INFO] Battery ADC: raw=2500 (61.0%), V_adc=2.012V, V_battery=11.85V
   ```

## ⚠️ Важные замечания

### ✅ Преимущества симуляции:
- Робот работает **прямо сейчас**
- Не нужны резисторы
- Можно тестировать веб-интерфейс
- Визуализация изменения заряда

### ❌ Ограничения:
- **НЕ показывает реальный заряд батареи!**
- Разрядка виртуальная (батарея может разрядиться по-настоящему)
- Нет защиты от полной разрядки

### 🚨 ВНИМАНИЕ:
**Следите за реальным зарядом батареи!**
- Используйте мультиметр периодически
- Заряжайте батарею когда напряжение <10V
- Li-Po батареи нельзя разряжать ниже 9V!

## 🎯 Когда отключить симуляцию

Отключите симуляцию когда:
- ✅ Подключили делитель напряжения (100kΩ + 33kΩ)
- ✅ Проверили мультиметром: напряжение на GPIO1 = 2-3V
- ✅ ADC показывает raw > 1500
- ✅ Хотите реальный мониторинг батареи

## 🧪 Тестирование симуляции

### Быстрый тест цветов индикатора:

Временно измените начальное значение для проверки всех цветов:

```cpp
// Высокий заряд (зелёный)
static float simulated_voltage = 11.8f;

// Средний заряд (жёлтый)
static float simulated_voltage = 10.5f;

// Низкий заряд (оранжевый)
static float simulated_voltage = 9.7f;

// Критический (красный мигающий)
static float simulated_voltage = 9.2f;
```

Загружайте код с каждым значением и смотрите на индикатор!

## 📞 Поддержка

### Если симуляция не работает:

1. Проверьте Serial Monitor - должно быть:
   ```
   Battery (SIMULATED): 11.80V
   ```

2. Проверьте веб-интерфейс - индикатор должен показать напряжение

3. Нажмите F12 в браузере → Console → найдите ошибки JavaScript

### Если нужна помощь:
Пришлите вывод Serial Monitor и скриншот веб-интерфейса!

---

## 📊 Сравнение режимов

| Функция | Симуляция | Реальное чтение |
|---------|-----------|-----------------|
| Работает без делителя | ✅ Да | ❌ Нет |
| Показывает реальный заряд | ❌ Нет | ✅ Да |
| Защита от разрядки | ❌ Нет | ✅ Да |
| Нужны резисторы | ❌ Нет | ✅ Да (100k+33k) |
| Стоимость | 🆓 Бесплатно | ~100₽ (резисторы) |
| Время настройки | ⚡ 1 минута | 🔧 5 минут |

---

**Текущий статус: 🔋 РЕЖИМ СИМУЛЯЦИИ АКТИВЕН**

Код загружен и готов к использованию! Загрузите на ESP32 и проверьте индикатор на веб-странице! ✨

