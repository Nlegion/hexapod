# 🔋 Устранение проблемы: индикатор показывает 0.03V

## 🔍 Диагностика проблемы

Если индикатор батареи показывает **0.03V** или другое очень низкое значение, это означает что ADC читает почти нулевое напряжение. Давайте найдём причину!

## 📊 Шаг 1: Диагностическая команда

1. **Откройте веб-интерфейс робота**
2. **Нажмите кнопку "🔋 Battery Check"** (в секции Testing & Diagnostics)
3. **Откройте Serial Monitor** (115200 baud) и найдите вывод:

```
=== BATTERY DIAGNOSTIC ===
Pin: 1
Divider: 4.20
ADC Ref: 3.30V
ADC Resolution: 4095
Battery ADC: raw=10 (0.2%), V_adc=0.008V, V_battery=0.03V
...
=========================
```

**Ключевые параметры для анализа:**
- **raw** - сырое значение ADC (должно быть 500-3000 для нормальной батареи)
- **V_adc** - напряжение на ADC пине (должно быть 1.5-3.0V)
- **V_battery** - вычисленное напряжение батареи

## 🔧 Возможные причины и решения

### Причина 1: Делитель напряжения не подключен ❌

**Симптомы:**
- ADC raw ≈ 0-50 (очень низкое)
- V_adc ≈ 0.00-0.05V

**Решение:**
Подключите делитель напряжения к пину A0 (GPIO1 на ESP32-S3):

```
Батарея (+12V) ----[ R1 = 100kΩ ]----+----[ R2 = 33kΩ ]---- GND
                                      |
                                  GPIO1 (A0)
```

**Проверка:**
- Используйте мультиметр для измерения напряжения между ADC_PIN и GND
- Должно быть около 2.5-3.0V для заряженной 3S батареи

---

### Причина 2: Неправильный ADC пин в коде ⚙️

**Симптомы:**
- Делитель подключен, но показывает 0V
- Pin в диагностике показывает неправильный номер

**Решение:**
Проверьте в `config.h`:

```cpp
constexpr int BATTERY_PIN = A0;  // Для ESP32-S3 это GPIO1
```

**Альтернативные ADC пины ESP32-S3:**
- `A0` = GPIO1
- `A1` = GPIO2
- `A2` = GPIO3
- `GPIO4` - `GPIO10` (можно использовать напрямую)

Попробуйте изменить на другой пин, например:
```cpp
constexpr int BATTERY_PIN = GPIO4;  // Или другой доступный пин
```

---

### Причина 3: Батарея не подключена 🔌

**Симптомы:**
- ADC raw ≈ 0
- Резисторы на месте, но напряжения нет

**Решение:**
- Убедитесь, что батарея подключена к делителю
- Проверьте мультиметром напряжение на батарее
- Проверьте предохранители и выключатели

---

### Причина 4: Неправильные резисторы делителя 📐

**Симптомы:**
- ADC raw очень низкое (<100)
- Резисторы слишком большие или R2 не тот

**Проверка резисторов:**
Измерьте мультиметром:
1. R1 между батареей+ и ADC_PIN
2. R2 между ADC_PIN и GND

**Типичные ошибки:**
- Перепутаны местами R1 и R2
- Использованы резисторы МОм вместо кОм
- Плохой контакт

**Пересчёт коэффициента:**
```
VOLTAGE_DIVIDER = (R1 + R2) / R2

Примеры:
- R1=100kΩ, R2=33kΩ → (100+33)/33 = 4.03
- R1=100kΩ, R2=30kΩ → (100+30)/30 = 4.33
- R1=220kΩ, R2=68kΩ → (220+68)/68 = 4.24
```

---

### Причина 5: ADC пин повреждён или занят 💥

**Симптомы:**
- Всё подключено правильно, но ADC всё равно читает 0
- Мультиметр показывает нормальное напряжение на пине

**Решение:**
Переключитесь на другой ADC пин:

```cpp
// В config.h измените:
constexpr int BATTERY_PIN = GPIO5;  // Попробуйте GPIO4-10
```

**Проверьте конфликты пинов:**
- A0 (GPIO1) не используется для Serial1 TX/RX
- Пин не настроен как OUTPUT в другом месте кода

---

## 🧪 Тестирование без батареи

### Тестовый режим с фиксированным напряжением:

Временно замените в `hexapod.ino`:

```cpp
float read_battery_voltage() {
  // ТЕСТОВЫЙ РЕЖИМ - возвращаем фиксированное значение
  return 11.5f;  // Симуляция 11.5V
  
  // Раскомментируйте после проверки:
  /*
  int adc_sum = 0;
  for (int i = 0; i < 10; i++) {
    adc_sum += analogRead(BATTERY_PIN);
    delayMicroseconds(100);
  }
  int adc_value = adc_sum / 10;
  float voltage = (float)adc_value / ADC_RESOLUTION * ADC_REF_VOLTAGE * VOLTAGE_DIVIDER;
  Logger::log(Logger::INFO, "Battery ADC: raw=%d (%.1f%%), V_adc=%.3fV, V_battery=%.2fV", 
              adc_value, 
              (float)adc_value / ADC_RESOLUTION * 100,
              (float)adc_value / ADC_RESOLUTION * ADC_REF_VOLTAGE,
              voltage);
  return voltage;
  */
}
```

Если индикатор показывает 11.5V после этого изменения - проблема в **аппаратном подключении**.

---

## 🔌 Схема правильного подключения

### Минимальная схема (без защиты):

```
     Батарея+
        |
    [ R1 = 100kΩ ]
        |
        +----------> ADC_PIN (A0 = GPIO1)
        |
    [ R2 = 33kΩ ]
        |
       GND
```

### Рекомендуемая схема (с защитой):

```
     Батарея+
        |
    [ R1 = 100kΩ ]
        |
        +---- C1 (0.1μF) ---- GND  (фильтр шума)
        |
        +----------> ADC_PIN (A0)
        |
        |--- Диод Шоттки к GND (защита)
        |
    [ R2 = 33kΩ ]
        |
       GND
```

**Компоненты:**
- R1: 100 кОм (коричневый-чёрный-жёлтый-золотой)
- R2: 33 кОм (оранжевый-оранжевый-оранжевый-золотой)
- C1: 0.1 мкФ керамический (опционально)
- Диод: 1N5819 или аналог (опционально)

---

## 📝 Быстрая проверка мультиметром

### Что измерять:

1. **Напряжение батареи** (между + и -)
   - Должно быть: 9-13V для 3S Li-Po
   - Если <9V → зарядите батарею

2. **Напряжение на R1** (между батарея+ и ADC_PIN)
   - Должно падать напряжение

3. **Напряжение на ADC_PIN** (между ADC_PIN и GND)
   - Должно быть: 2.0-3.2V для нормальной батареи
   - **ВАЖНО:** Не более 3.3V!

4. **Сопротивление R1 и R2**
   - R1 ≈ 100 кОм
   - R2 ≈ 33 кОм

---

## 🎯 Пошаговая диагностика

### ✅ Checklist:

- [ ] Батарея подключена и заряжена (>9V)
- [ ] R1 = 100kΩ установлен между Bat+ и ADC_PIN
- [ ] R2 = 33kΩ установлен между ADC_PIN и GND
- [ ] ADC_PIN правильно подключен к GPIO1 (A0)
- [ ] Мультиметр показывает 2-3V на ADC_PIN
- [ ] В config.h: `BATTERY_PIN = A0`
- [ ] В config.h: `VOLTAGE_DIVIDER = 4.03` (или правильный для ваших резисторов)
- [ ] Serial Monitor показывает ADC raw > 1000
- [ ] Нажата кнопка "🔋 Battery Check" для диагностики

---

## 💡 Если ничего не помогает

### Альтернативный подход - без делителя (для тестирования):

Если у вас есть источник питания 3.3V (или USB-порт с регулятором):

1. **Временно подключите 3.3V** напрямую к ADC_PIN
2. **Установите** `VOLTAGE_DIVIDER = 1.0f` в config.h
3. **Индикатор должен показать** 3.3V

Если это работает → проблема в делителе напряжения.
Если нет → проблема в коде или настройке ADC.

---

## 📞 Дополнительная помощь

Если проблема остаётся, отправьте мне вывод команды **BATTERY_CHECK**:

```
=== BATTERY DIAGNOSTIC ===
Pin: ?
Divider: ?
ADC Ref: ?
ADC Resolution: ?
Battery ADC: raw=? (?.?%), V_adc=?.???V, V_battery=?.??V
...
```

И я помогу найти причину!

---

**Важно:** Всегда проверяйте, что напряжение на ADC_PIN не превышает 3.3V!  
Это может повредить ESP32! ⚡

