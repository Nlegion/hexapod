# ⚡ 3 шага для исправления

## 📊 Ваша ситуация

```
✅ ADC работает: raw = 140
❌ Напряжение слишком низкое: 0.11V (ожидается 2-3V)
❌ Вычисленное напряжение: 0.5V (ожидается 11-12V)
```

**Вывод:** Делитель напряжения не подключен или батарея отключена.

---

## 🔧 ШАГ 1: Проверьте батарею (30 секунд)

### С мультиметром:
```
Мультиметр → DC Voltage
Красный → Bat+
Чёрный → Bat-
```

**Должно показать: 9-13V**

### Без мультиметра:
- Батарея включена? (проверьте выключатель)
- Светятся ли индикаторы на роботе?
- Моторы работают?

Если батарея <9V → **зарядите её**.

---

## 🔧 ШАГ 2: Проверьте делитель (2 минуты)

### Нужна эта схема:

```
   Батарея+
      │
    ┌─┴─┐
    │100│  ← Резистор R1 = 100 кОм
    │кΩ │     (коричн-чёрн-жёлтый)
    └─┬─┘
      │
      ├────────→ GPIO1 (A0) на ESP32
      │
    ┌─┴─┐
    │33 │  ← Резистор R2 = 33 кОм  
    │кΩ │     (оранж-оранж-оранж)
    └─┬─┘
      │
     GND
```

### Проверьте:
- [ ] R1 соединяет Bat+ и точку между резисторами
- [ ] R2 соединяет точку между резисторами и GND
- [ ] GPIO1 подключен к точке между R1 и R2
- [ ] Все провода надёжно закреплены

### С мультиметром:
Измерьте напряжение между GPIO1 и GND:
- **Должно быть: 2-3V**
- **У вас сейчас: 0.11V** ← ПРОБЛЕМА!

---

## 🔧 ШАГ 3А: Если делитель НЕ подключен

### Соберите делитель:

**Вам нужно:**
- 1× резистор 100 кОм (или 82-120 кОм)
- 1× резистор 33 кОм (или 30-39 кОм)
- 3 провода
- Breadboard или прямая пайка

**Подключение:**
1. Bat+ → один конец R1
2. Другой конец R1 → GPIO1 + один конец R2
3. Другой конец R2 → GND

**После подключения:**
- Перезагрузите ESP32
- ADC должен показать 2000-3000
- Индикатор покажет 10-13V ✅

---

## 🔧 ШАГ 3Б: Если нет резисторов СЕЙЧАС

### Временная симуляция:

В `hexapod.ino` найдите функцию `read_battery_voltage()` (строка ~610):

**Замените на:**
```cpp
float read_battery_voltage() {
  // ВРЕМЕННАЯ СИМУЛЯЦИЯ - пока нет делителя
  // Удалите это после подключения реального делителя!
  return 11.5f;  // Возвращает фиксированное значение
}
```

**Загрузите код:**
- Индикатор покажет 11.5V
- Робот будет работать
- **Но это НЕ реальные данные!**

**Когда подключите делитель:**
Верните оригинальный код:
```cpp
float read_battery_voltage() {
  int adc_sum = 0;
  for (int i = 0; i < 10; i++) {
    adc_sum += analogRead(BATTERY_PIN);
    delayMicroseconds(100);
  }
  int adc_value = adc_sum / 10;
  float voltage = (float)adc_value / ADC_RESOLUTION * ADC_REF_VOLTAGE * VOLTAGE_DIVIDER;
  Logger::log(Logger::INFO, "Battery ADC: raw=%d (%.1f%%), V_adc=%.3fV, V_battery=%.2fV", 
              adc_value, 
              (float)adc_value / ADC_RESOLUTION * 100,
              (float)adc_value / ADC_RESOLUTION * ADC_REF_VOLTAGE,
              voltage);
  return voltage;
}
```

---

## 📊 Как понять что заработало

После подключения делителя Serial Monitor должен показать:

```
[INFO] Battery ADC: raw=2500 (61.0%), V_adc=2.012V, V_battery=11.85V
[INFO] Battery status sent: 11.85V
```

**Признаки успеха:**
- ✅ raw > 1500 (было ~140)
- ✅ V_adc > 2.0V (было 0.11V)
- ✅ V_battery > 9.0V (было 0.5V)
- ✅ Индикатор на веб-странице зелёный

---

## 🛒 Где купить резисторы (если нужно)

### Онлайн:
- **Aliexpress**: "100k 33k resistor kit" (~50-100₽)
- **Радиомагазин.рф**: поиск "резистор 100 кОм"
- **Местный магазин электроники**

### Альтернативные номиналы:
Если нет точно 100k и 33k, можно:
- **R1**: 82k, 100k, 120k, 150k
- **R2**: 27k, 30k, 33k, 39k

**Пересчитайте коэффициент:**
```cpp
VOLTAGE_DIVIDER = (R1 + R2) / R2

Примеры:
R1=120k, R2=33k → (120+33)/33 = 4.64
R1=100k, R2=30k → (100+30)/30 = 4.33
```

---

## 🎯 Краткий вывод

**Проблема:** Делитель напряжения не подключен к батарее.

**Решение:** 
1. ✅ Соберите делитель (100кΩ + 33кΩ)
2. ✅ Подключите к Bat+, GPIO1, GND
3. ✅ Перезагрузите ESP32
4. ✅ Проверьте: ADC raw должно быть >1500

**Или временно:** Используйте симуляцию (return 11.5f) пока не купите резисторы.

---

## 📞 Всё ещё не работает?

Пришлите мне:
1. Фото вашей схемы подключения
2. Напряжение батареи (мультиметр)
3. Вывод Serial Monitor после подключения

Я помогу найти проблему!

